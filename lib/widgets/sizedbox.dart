import 'package:flutter/material.dart';

sbh( height) {
  var size = double.parse(height.toString());
  return SizedBox(
    height: size,
  );
}

sbw(width) {
   var size = double.parse(width.toString());
  return SizedBox(
    width: size,
  );
}
